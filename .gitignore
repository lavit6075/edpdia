# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Generated by prebuild from schools.json — regenerated on every build
scripts/routes.generated.json

# Ad-hoc measurement/verification scripts run from the repo root so they can resolve
# playwright out of node_modules. Never part of the build.
*.tmp.mjs

# Generated by scripts/modernise-images.mjs — a per-image encode report, useful for auditing a
# run but noisy in diffs. The manifest it also writes (src/data/image-variants.json) IS tracked,
# because the app imports it.
scripts/image-report.generated.json
