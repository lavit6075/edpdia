# Playwright downloads Chromium into node_modules instead of a home-dir cache, so Vercel's
# node_modules caching persists it between builds and we don't re-download ~95MB every deploy.
playwright_browsers_path=0
