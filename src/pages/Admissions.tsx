import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function Admissions() {
  return (
    <PagePlaceholder
      title="Admissions Guide"
      description="General guidance on steps, timelines, checklists, and exam types (ISEB, CAT4, MAP, 11+, 13+)."
    />
  );
}
